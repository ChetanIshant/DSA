## Two Pointers:
This approach/pattern includes solving the typical array problems by using two pointers moving across the array.
Use this approach for sorted array problems.
For unsorted array, if you use two pointer approach it will be same as using a loop inside another loop.
